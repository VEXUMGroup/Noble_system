# 2026-07-16 退職予定日カレンダー選択化

## 対象
- 新規商談登録画面 `nobledashboard/deal-app/src/app/(authenticated)/deals/new/page.tsx`

## 変更内容
- `退職予定日` の入力欄を自由入力テキストから日付選択UIへ変更する
- 既存の `商談日` と同じくブラウザ標準の `type="date"` を利用する

## 目的
- 入力形式の揺れを防ぐ
- 新規登録時の日付入力をカレンダー選択で行えるようにする

## 非対象
- 商談詳細編集画面の `退職予定日`
- カレンダー連携モーダルの入力仕様変更
